# =============================================================================
# Voice Agent Environment Configuration
# Copy to .env and fill in values
# =============================================================================

# HuggingFace Token (required for downloading Qwen3, Chatterbox)
# Get from: https://huggingface.co/settings/tokens
HF_TOKEN=

# GPU Configuration
CUDA_VISIBLE_DEVICES=0

# =============================================================================
# Triton + vLLM Configuration
# All models are served through a single Triton server with vLLM backend
# =============================================================================

# LLM Model (Qwen3 via vLLM - downloaded automatically from HuggingFace)
# The model is configured in model_repository/llm/qwen3/1/model.json
# Default: cpatonn/Qwen3-4B-Instruct-2507-AWQ-4bit (4-bit quantized, ~3GB VRAM)

# T3 Model (Chatterbox speech token generator via vLLM)
# Weights must be downloaded first: ./scripts/build_chatterbox.sh

# =============================================================================
# Logging
# =============================================================================
LOG_LEVEL=INFO
TRITON_LOG_LEVEL=INFO

# =============================================================================
# TTS Configuration
# =============================================================================
TTS_BACKEND=chatterbox
TTS_MODEL=chatterbox

# Voice cloning (optional)
# REFERENCE_AUDIO_PATH=/app/voice.wav
# REFERENCE_TEXT=The quick brown fox jumps over the lazy dog.
